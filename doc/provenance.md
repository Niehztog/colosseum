# Provenance

Where every file in this tree came from, at which commit, and under which
licence. Required by R-CORE-9, R-PROV-1, R-PROV-5 and R-LIC-5, and by Q1's
closure ("pin `b1f2c18f` and record it in `doc/provenance.md`" — re-pinned in
SPECS.md 1.4, see below).

Written at Phase 0. A later phase that imports a donor adds its row here in the
same commit as the import.

## 1. Pins

All paths relative to `<workspace>`. Re-verified 2026-08-21.

| what | source | pinned commit |
|---|---|---|
| `inc/shared`, `inc/common`, `inc/format` | `q2pro/inc` | `c751d316845ac4b27456d5cbd97f837979b24b26` |
| `src/` (72 files) | `q2pro/src/game` | same |
| `src/shared/m_flash.c`, `src/shared/shared.c` | `q2pro/src/shared` | same |
| `LICENSE` | `q2pro/LICENSE` | same |
| `tools/` (30 scripts) | `q2pro-mission-pack-tools` | `66f043d` (branch `ubuntu`) |
| `vendor/harness/` | `../colosseum-harness-archive` | not a git tree; see its `MANIFEST.md` |

`q2pro` is on branch `feature/mission-packs`. `c751d316` is its tip and is a
**README-only** commit; `3ffc4642` ("Mission packs: portability and latent-bug
fixes") is the last commit that touches `src/`. The tip is pinned for
reproducibility and this distinction is recorded per R-PROV-5 clause 1, so that
a future re-pin can tell a content change from a documentation one.

### 1.1 Superseded identifiers

SPECS.md 1.0–1.3 pinned commits from a WSL working copy under
`/mnt/c/Users/<user>/q2-dev`. None of them resolve on this machine: the donor
repositories were re-committed, with identical content and identical commit
shapes but new object names. Recorded so the old spec text stays traceable
(SPECS.md §3.3, R-PROV-5):

| donor | 1.0–1.3 | 1.4 |
|---|---|---|
| `q2pro` | `b1f2c18f` (`r1504-2231-gb1f2c18f`) | `c751d316` |
| `rocketarena2-public` | `c7d0f3a` | `cd0708b` |
| `osp-tourney` | `ab0e13a` | `a8e30d0` |

`789895b` — the RA2 commit R-RA-1a and R-OSP-10 name for the GameSpy removal —
still resolves and is not re-pinned. Q1's closure paragraph in SPECS.md §12
names three SHAs that no longer exist; the decision it records stands, the
identifiers do not.

### 1.2 Bundle refs — the durable half of the pin

R-PROV-6. A bundle carries objects, so its names cannot drift the way the live
branches did. Verified byte-exact 2026-08-21:

| ref | SHA | bundle |
|---|---|---|
| `C0` (root, id 3.20/3.21 normalised) | `7ceeeed` | all three |
| `baseq2` (the spine, 188 Q2PRO game commits) | `5d180cb` | all three |
| `port_ctf` | `e94bbeb` | `q2pro-mission-pack-replay` |
| `port_xatrix` | `2829dba` | `q2pro-mission-pack-replay` |
| `port_rogue` | `2ebed93` | `q2pro-mission-pack-replay` |
| `port_ra2` | `7322c9f` | `ra2-q2pro-port-replay` |
| `port_ra2` | `3f5ecb9` | `osp-q2pro-port-replay` (one commit further) |
| `port_osp` | `205a89c` | `osp-q2pro-port-replay` |

`tools/divergence.py` and `tools/coverage.py` use `port_ra2` = `3f5ecb9`, the
further of the two, and print the SHA they used so the choice is never implicit.

### 1.3 Harness archive

R-PROV-2a/2b, R-PROV-7, R-TOOL-4. Verified 2026-08-21:

* `vendor/harness/rr-cache/ra2replay/` — **411** recorded `rerere` resolutions
* `vendor/harness/rr-cache/replay-missionpacks/` — **85**
* `vendor/harness/tools-inputs/commit_paths.txt` — 188 lines; **all 188 SHAs
  still resolve** against `q2pro`. They are upstream Q2PRO commits and survived
  the re-commit that moved the three pins in §1.1.
* `vendor/harness/notes/TODO.md` — the harness author's eight open items, folded
  into SPECS.md as R-VER-15.

Deliberately **not** vendored: `dl/` (12 MB of id's five official source
releases — public, reobtainable) and `astyle/astyle-3.1` (R-PROV-2b says not to
commit it; R-BUILD-6 records that the host's native `astyle` is the same version
and produces byte-identical output, so it is not needed).

## 2. Per-file provenance and licence

| path | origin | licence |
|---|---|---|
| `src/*.{c,h}` except `g_ptrs.c` | `q2pro/src/game`, byte-identical (R-CORE-5) | GPL-2-or-later, id-derived (R-LIC-1) |
| `src/g_ptrs.c` | generated by `src/genptr.py` over `src/` (R-SAVE-2, R-BUILD-4) | GPL-2-or-later |
| `src/genptr.py` | `q2pro/src/game/genptr.py`, byte-identical | GPL-2-or-later |
| `src/shared/*.c` | `q2pro/src/shared`, byte-identical | GPL-2-or-later |
| `inc/**` | `q2pro/inc`, byte-identical, never edited (R-CORE-9) | GPL-2-or-later |
| `config.h` | written for this tree, modelled on `osp-tourney/config.h` | GPL-2-or-later |
| `Makefile`, `meson.build` | written for this tree | GPL-2-or-later |
| `tools/*.py`, `tools/*.sh` | `q2pro-mission-pack-tools`, with the path repairs of R-TOOL-1 | GPL-2-or-later |
| `tools/{divergence,counts,coverage,audit}.py` | written for this tree (R-TOOL-5) | GPL-2-or-later |
| `src/arena/*.{c,h}` | `rocketarena2-public@d20e1ce`, asm-matching comments stripped and the GPL header restored (`doc/reconciliation.md` R-65, R-67) | GPL-2-or-later, id-derived (R-LIC-1) |
| `src/tourney/*.{c,h}` | `osp-tourney@1d8427e`, same treatment (R-78, R-84) | GPL-2-or-later, id-derived (R-LIC-1) |
| `src/bot/{botlib,bl_main,bl_botcfg,bl_cmd,bl_debug,bl_redirgi,bl_spawn}.h`, `src/bot/bl_*.c` | `osp-tourney@1d8427e`, same treatment as `src/tourney/`. Headers only in Phase 5, because R-OSP-5 needs `botglobals` declared exactly once (R-86); the six implementations landed in Phase 6. One slot of `botlib.h` gains a second spelling there — see `doc/botlib-contract.md` version 2 and reconciliation R-97 | GPL-2-or-later, Gladiator SDK (R-LIC-2) |
| `src/bot/p_menulib.{c,h}`, `src/bot/p_botmenu.{c,h}` | `gladiator-bot-restored@game`, which `osp-tourney` cannot supply: it moved its bot menu into `osp_menus.c` on id's `PMenu` and ships neither file (R-BOT-28). The two reconstructions' copies are byte-identical, so there is one source of truth | non-commercial, Gladiator (R-LIC-2) |
| `src/g_fs.c` | written for this tree — the engine's extended API as the game library sees it (§5.7, R-BOT-8, R-BOT-26, reconciliation R-95) | GPL-2-or-later |
| `tools/{botabi.py,botmatrix.sh}` | written for this tree (R-VER-3, R-VER-28) | GPL-2-or-later |
| `vendor/replay/*.bundle` | the replay harness (R-PROV-1) | contains id-derived GPL-2 sources |
| `vendor/harness/**` | rescued harness scratchpad (R-PROV-2a) | mixed; see its `MANIFEST.md` |
| `LICENSE` | `q2pro/LICENSE`, GPL-2 verbatim | — |

**RA2's pin moves in spec 1.16, from `cd0708b` to `d20e1ce`** — one commit, *"Build
with -Wall, and clear what it reports"*. Colosseum builds `-Wall -Wextra -Werror`,
so its four fixes are needed here or they get rediscovered, and §7 rule 7 makes
them ours. One of the four is **refused**: `barrel_touch`'s dead locals are
removed there because RA2 ships no `M_walkmove`, and this tree does
(`doc/reconciliation.md` R-66).

**Tourney's pin moves in spec 1.19, from `a8e30d0` to `1d8427e`** — two commits,
*"Fix the timer-unit defects the -w build hid, and turn -Wall on"* and *"Bound the
bot network message buffer"*. Both were named in §3.3 as work to carry, and §7
rule 7 makes them ours; taking the tip is cheaper and more honest than
cherry-picking their halves.

`gladiator-bot-restored` arrives in Phase 6, and only its two menu files —
`p_menulib.c` and `p_botmenu.c`, rows above. Its `botlib/` is **not vendored**:
R-BOT-3 loads the brain dynamically and R-BOT-4 says it is compiled from source
for whichever platform the game targets, so it stays a sibling repository and
`tools/botmatrix.sh` takes its path as `GLADDIR`. `tools/botabi.py` reads one
header out of it in `make check` and skips itself with a message when it is not
there.

Nothing from `gladq2_src` or `ugladq2` is in the tree. `gladq2_src/g_arena.c` is
refused outright by R-ARENA-1 (R-LIC-3, the RA2 Bot Support Routines).

## 3. A vendored header that is not closed under `#include`

`inc/common/bsp.h` line 24 includes `system/hunk.h`, and `inc/system/` is not
vendored — SPECS.md §5.2 lists three directories under `inc/` and that is what
was taken. This is harmless and deliberate: the game library never includes
`common/bsp.h`, so the dangling include is never followed. Recorded rather than
fixed, because vendoring a fourth directory would deviate from §5.2 and editing
the header would violate R-CORE-9's "never edited". If a later phase needs
`common/bsp.h`, vendor `inc/system/hunk.h` verbatim then and amend §5.2.
